import SwiperCore, { Autoplay, Navigation, Pagination } from "swiper/core";
import "swiper/swiper.scss";
SwiperCore.use([Autoplay, Pagination, Navigation]);

const SwiperJSConfig = () => {
  return <></>;
}

export default SwiperJSConfig;